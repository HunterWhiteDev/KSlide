KSlide uses the following model:

Everything is contained in a gird. Within that grid you have rows and columns. A row contains an infinite amount of columns. Every Column is touching each other however, padding conforms the windows within a column to the width of a column minus the padding.
